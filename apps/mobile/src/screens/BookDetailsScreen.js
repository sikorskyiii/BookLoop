import { View, Text } from 'react-native';

export default function BookDetailsScreen() {
  return (
    <View style={{ flex:1, alignItems:'center', justifyContent:'center' }}>
      <Text>Book Details (stub)</Text>
    </View>
  );
}
